---
import type { FeaturedItem } from "../config/home";
import { Image } from "astro:assets";
import TypeBadge from "./TypeBadge.astro";

type Props = FeaturedItem;

const { type, title, description, href, featuredImage, featuredImageAlt } =
    Astro.props;
---

<a
    href={href}
    class="group flex flex-col bg-white rounded-xl border border-border hover:shadow-[0_2px_12px_rgba(15,23,42,0.06)] transition-shadow overflow-hidden"
>
    {
        featuredImage && (
            <div class="aspect-[3/2] overflow-hidden">
                <Image
                    src={featuredImage}
                    alt={featuredImageAlt ?? ""}
                    class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-[1.02]"
                    widths={[480, 800]}
                    sizes="(min-width: 1024px) 480px, (min-width: 640px) 50vw, 100vw"
                />
            </div>
        )
    }

    <div class="flex flex-col flex-1 p-6">
        <div class="self-start mb-4">
            <TypeBadge type={type} />
        </div>

        <!-- Card title: Manrope 16px / semibold / -0.015em -->
        <h3
            class="font-heading text-base font-semibold tracking-[-0.015em] text-text-primary leading-[1.25] mb-2 group-hover:text-accent transition-colors"
        >
            {title}
        </h3>

        <!-- Body: Inter 14px / normal / 1.65 -->
        <p
            class="text-sm text-text-secondary leading-[1.65] flex-1 line-clamp-3"
        >
            {description}
        </p>

        <!-- Link: Inter 12px / medium -->
        <span
            class="mt-4 text-xs font-medium text-accent group-hover:opacity-75 group-hover:underline underline-offset-[3px] transition-opacity"
            aria-hidden="true"
        >
            Read more →
        </span>
    </div>
</a>
